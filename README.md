# RadiFlow Player

RadiFlow Player is a desktop music player focused on immersive playback, synced lyrics, local library browsing, and playlist management.

## Run Locally

Prerequisite: Node.js

1. Install dependencies: `npm install`
2. Start the local dev server: `npm run dev`
3. Start the Electron desktop shell: `npm run electron:dev`
